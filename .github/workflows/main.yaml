name: demo
on:
  workflow_dispatch:
jobs:
  job_demo:
    runs-on: ubuntu-latest
    steps:
      - name: Run a simple command
        run: echo "Hello from GitHub Actions"

  job_B:
    runs-on: ubuntu-latest
    needs: [job_demo]
    steps:
      - run: df -h

  job_c:
    runs-on: ubuntu-latest
    needs: [job_B]
    steps:
      - run: free -h
